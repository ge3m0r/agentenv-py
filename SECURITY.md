# Security

`LocalProcessBackend` **不提供安全隔离**。通过 API 或 CLI 提交的命令会以
AgentENV Python 服务进程当前用户的权限在本机执行。

`DockerSandboxBackend` 提供容器边界和资源限制，但默认 Docker 配置不应
被视为对抗恶意多租户的完整安全边界。当前服务也没有身份认证或租户授权。

- 不要将当前版本暴露到公网；
- 只在可信开发环境和可信输入下运行；
- 生产使用前请实现具备安全边界的容器或虚拟机后端，并增加认证、授权、
  网络策略和资源限制。

发现安全问题时，请通过远程仓库的私密安全报告渠道联系维护者，不要公开
披露利用细节。
